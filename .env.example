# Environment Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Admin password for authentication AND JWT secret
# This is used for:
# 1. Validating admin login credentials
# 2. Signing JWT tokens (30-day expiration)
# WARNING: Changing this will invalidate all existing JWT sessions
ADMIN_PASSWORD=your_secure_password_here

# =============================================================================
# OPTIONAL VARIABLES (with defaults)
# =============================================================================

# Server Configuration
# Port the server will listen on
# Default: 3000
PORT=3000

# Node environment (development, production, test)
# Default: development
NODE_ENV=development

# Your domain name (used for Open Graph meta tags and HTTPS)
# Default: localhost
# Production example: yourdomain.com
# Used for generating social media preview links
DOMAIN=localhost

# Database Configuration
# Path to SQLite database file (will be created if doesn't exist)
# Default: ./data/database.sqlite
DB_PATH=./data/database.sqlite

# Logging Configuration
# Winston log level: error, warn, info, debug, verbose, silly
# Default: info
# Production recommendation: warn
LOG_LEVEL=info

# Security Configuration
# CORS allowed origins (comma-separated or * for all)
# Default: *
# Production recommendation: https://yourdomain.com
CORS_ORIGIN=*

# Rate limiting (requests per minute)
# Admin endpoints rate limit
# Default: 10
ADMIN_RATE_LIMIT=10

# Public endpoints rate limit
# Default: 60
PUBLIC_RATE_LIMIT=60

# =============================================================================
# USAGE NOTES
# =============================================================================
# 
# Development: Copy this file to .env and use relaxed settings
# Production:  Copy .env.production to .env and customize with secure values
#
# Never commit .env files to version control!
